# Replicate API (https://replicate.com/account/api-tokens)
# Copy to `.env` in this directory or export in your shell before running generators.
REPLICATE_API_TOKEN=

# Optional tuning for generate_image.py (same semantics as yieldomega replicate-art helpers):
# REPLICATE_MAX_GENERATION_SECONDS=600
# REPLICATE_CREATE_WAIT_SECONDS=1
