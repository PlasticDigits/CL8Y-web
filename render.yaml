services:
  - type: web
    name: CL8Y-web
    runtime: static
    buildCommand: yarn install && yarn build
    staticPublishPath: ./dist
    envVars:
      - key: SKIP_INSTALL_DEPS
        value: "true"
      - key: NODE_VERSION
        value: "24"
